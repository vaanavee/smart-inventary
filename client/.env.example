# Only needed for production builds (local dev uses Vite's proxy instead).
# Set to the deployed backend's public URL, no trailing slash, e.g.:
VITE_API_BASE_URL=https://smart-inventary-api.onrender.com
